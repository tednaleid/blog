# ABOUTME: direnv environment for the blog; mise already supplies the ruby from .ruby-version.
# ABOUTME: This only adds what mise does not, so `bundle exec` works from anywhere in the repo.

export BUNDLE_GEMFILE="$PWD/docs/Gemfile"
